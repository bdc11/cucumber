

1. Install RVM stable with ruby:   
`\curl -sSL https://get.rvm.io | bash -s stable --ruby`
2. `source /Users/<YOUR_USER>/.rvm/scripts/rvm`
3. `rvm install 2.4.2`
4. `rvm use --default ruby-2.4.2`
5. Install bundler: `gem install bundler`
